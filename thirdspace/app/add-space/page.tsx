import AddSpaceForm from "./AddSpace";

export default function BookmarkPage() {
  return (
    <main className="flex items-center justify-center h-full w-screen">
      <AddSpaceForm />
    </main>
  );
}